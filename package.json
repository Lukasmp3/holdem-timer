{
    "files": [
        "src/"
    ],
    "scripts": {
        "start": "parcel serve src/index.html",
        "build": "cross-env NODE_ENV=production parcel build --no-minify src/index.html --public-url .",
        "deploy": "gh-pages -d dist"
    },
    "devDependencies": {
        "@types/jest": "^26.0.22",
        "cross-env": "^5.2.0",
        "gh-pages": "^1.2.0",
        "jest": "^26.6.3",
        "parcel-bundler": "^1.9.7",
        "ts-jest": "^26.5.5",
        "typescript": "^4.2.4"
    }
}
